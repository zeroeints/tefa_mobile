import 'package:flutter/material.dart';
import '../widgets/bottomNavBar.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BottomNavBar(); // Panggil widget bottom navigation
  }
}
